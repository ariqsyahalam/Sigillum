# Example environment variables — copy to .env.local and fill in values.
# This file IS committed to git as a reference. Never commit .env.local itself.

# ── Required ───────────────────────────────────────────────────────────────────

# Full public base URL of this deployment (no trailing slash)
# Development: http://localhost:3000
# Production:  https://yourdomain.com
APP_BASE_URL=http://localhost:3000

# Admin bearer token for document upload and admin panel access
# Generate a strong random string: openssl rand -hex 32
DOCUCERT_ADMIN_TOKEN=

# ── Storage mode ───────────────────────────────────────────────────────────────
# local  — filesystem at /storage/documents/ (default for dev)
# r2     — Cloudflare R2 (production)
STORAGE_MODE=local

# Cloudflare R2 credentials (only needed when STORAGE_MODE=r2)
# R2_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
# R2_ACCESS_KEY=
# R2_SECRET_KEY=
# R2_BUCKET=sigillum-documents

# ── Database mode ──────────────────────────────────────────────────────────────
# sqlite   — local SQLite at /data/app.db (default for dev)
# supabase — Supabase Postgres (production)
DB_MODE=sqlite

# Supabase credentials (only needed when DB_MODE=supabase)
# SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=
